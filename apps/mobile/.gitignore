.dart_tool/
.packages
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
build/
*.iml
*.g.dart

# Platform projects are generated locally (see README.md) and left untracked
# on purpose: they must be built with `flutter create` against whatever
# Flutter/Gradle/Xcode toolchain the developer has installed.
android/
ios/
macos/
linux/
windows/
web/

.vscode/
.idea/
